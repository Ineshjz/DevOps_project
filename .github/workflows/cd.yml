name: CD - Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_INESHJZ04 }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push database image
        run: |
          docker build -t ineshjz04/tp-devops-database ./sample-application-students-master/database
          docker push ineshjz04/tp-devops-database

      - name: Build and push api image
        run: |
          docker build -t ineshjz04/tp-devops-simple-api ./sample-application-students-master/simple-api
          docker push ineshjz04/tp-devops-simple-api

      - name: Build and push httpd image
        run: |
          docker build -t ineshjz04/tp-devops-httpd ./sample-application-students-master/http-server
          docker push ineshjz04/tp-devops-httpd
