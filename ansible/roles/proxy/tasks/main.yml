---
- name: Launch HTTPD proxy container
  docker_container:
    name: httpd
    image: ineshjz04/tp-devops-httpd
    networks:
      - name: app-network
    ports:
      - "80:80"
    state: started
    restart_policy: always
  vars:
     ansible_python_interpreter: /opt/docker_venv/bin/python

